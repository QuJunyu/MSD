# Database connection configuration (When C checks, confirm that B has been modified to actual information)
db_config = {
    "host": "localhost",         # Local database uses localhost by default
    "user": "root",              # Your MySQL username (default is root)
    "password": "20040715",        # Change to your MySQL password! Connection will fail if not changed
    "database": "campus_book_trade",   # Database name (exclusively for login function)
    "port": 3306                 # MySQL default port
}
