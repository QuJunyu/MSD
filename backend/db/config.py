# Database connection configuration (When checking C, ensure that B has been modified to actual information)
db_config = {
    "host": "localhost",         # Local database defaults to localhost
    "user": "root",              # Your MySQL username (default is root)
    "password": "123456",        # Change to your MySQL password! Connection will fail if not changed
    "database": "campus_book_trade",   # Database name (exclusively for login function)
    "port": 3306                 # MySQL default port
}
