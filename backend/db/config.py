# Database connection configuration (must be modified to your MySQL information)
db_config = {
    "host": "localhost",
    "user": "root",              # Your MySQL username
    "password": "123456",        # Your MySQL password (must be changed!)
    "database": "campus_book",   # Database name (consistent with schema.sql)
    "port": 3306
}
